/**
 * SectionOrnament — three-dot accent break.
 *
 * A lightweight rhythm marker for dropping between sections of body copy
 * where a new H2 would be overkill. Purely decorative.
 */
export function SectionOrnament() {
  return (
    <div className="ornament" aria-hidden="true">
      <span />
    </div>
  );
}
